import { QuotationDTO } from '../types';
import { buildCompanyContact, getCachedCompanySettings } from '../utils/companySettings';
const esc=(v:any)=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
const money=(v:any)=>new Intl.NumberFormat('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(v)||0);
const date=(v?:string)=>v?new Date(v).toLocaleDateString('en-GB'):'-';
export const buildQuotationVoucherHtml=(q:QuotationDTO)=>{const cs=getCachedCompanySettings(); const rows=(q.details||[]).map((d,i)=>`<tr><td>${i+1}</td><td class="left">${esc(d.productName||`Product #${d.productId}`)}</td><td>${d.qty||0}</td><td>${money(d.unitPrice)}</td><td>${money(d.discountAmount)}</td><td>${money(d.subtotal)}</td></tr>`).join(''); return {popupSize:'width=900,height=900',html:`<!doctype html><html><head><meta charset="utf-8"><title>${esc(q.quotationCode)}</title><style>@page{size:A4;margin:14mm}body{font-family:Arial,sans-serif;color:#172033;margin:0}.head{text-align:center;border-bottom:2px solid #334155;padding-bottom:12px}.logo{max-height:65px;max-width:180px}.title{font-size:22px;font-weight:800;margin:14px 0}.meta{display:grid;grid-template-columns:1fr 1fr;gap:6px;font-size:13px;margin:16px 0}table{width:100%;border-collapse:collapse;font-size:12px}th,td{border:1px solid #cbd5e1;padding:7px;text-align:right}th{background:#f1f5f9}.left{text-align:left}.totals{margin:12px 0 0 auto;width:310px}.terms{margin-top:18px;border:1px solid #cbd5e1;padding:10px;font-size:12px;white-space:pre-wrap}.sign{display:flex;justify-content:space-between;margin-top:55px;font-size:12px}.print{position:fixed;right:15px;top:15px}@media print{.print{display:none}}</style></head><body><button class="print" onclick="window.print()">Print / PDF</button><div class="head">${cs.logoBase64?`<img class="logo" src="${cs.logoBase64}">`:''}<h1>${esc(cs.companyName||'Company')}</h1><div>${esc(buildCompanyContact(cs))}</div></div><div class="title">QUOTATION / ဈေးနှုန်းကမ်းလှမ်းချက်</div><div class="meta"><div><b>No:</b> ${esc(q.quotationCode)}</div><div><b>Date:</b> ${date(q.quotationDate)}</div><div><b>Customer:</b> ${esc(q.customerName)}</div><div><b>Valid Until:</b> ${date(q.validUntil)}</div><div><b>Status:</b> ${esc(q.status)}</div></div><table><thead><tr><th>#</th><th class="left">Description</th><th>Qty</th><th>Unit Price</th><th>Discount</th><th>Amount</th></tr></thead><tbody>${rows}</tbody></table><table class="totals"><tr><th>Subtotal</th><td>${money(q.totalAmount)}</td></tr><tr><th>Discount</th><td>${money(q.discountAmount)}</td></tr><tr><th>Net Amount</th><td><b>${money(q.netAmount)}</b></td></tr></table>${q.terms?`<div class="terms"><b>Terms & Conditions</b><br>${esc(q.terms)}</div>`:''}${q.remark?`<div class="terms"><b>Remark</b><br>${esc(q.remark)}</div>`:''}<div class="sign"><span>Prepared by __________________</span><span>Customer acceptance __________________</span></div></body></html>`};};
